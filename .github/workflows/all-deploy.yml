name: Deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  security-events: write
  id-token: write
  actions: read
  contents: read

jobs:
  security_scan:
    name: Trivy Vulnerability Scan
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Verify System Architecture
      run: |
        uname -a
        arch

    - name: Install Docker
      run: |
        sudo apt-get update
        sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/docker.gpg
        sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
        sudo apt-get update
        sudo apt-get install -y docker-ce docker-ce-cli containerd.io
        sudo systemctl start docker
        sudo systemctl enable docker

    - name: Pull Docker Image
      run: |
        docker pull shadowhub/cocomaster

    - name: Run Trivy Vulnerability Scanner
      uses: aquasecurity/trivy-action@0.30.0
      with:
        image-ref: 'shadowhub/cocomaster'
        format: 'sarif'
        output: 'trivy-results.sarif'
        severity: 'CRITICAL,HIGH'
        ignore-unfixed: true
        scanners: 'vuln'

    - name: Run KICS IaC Scan
      uses: checkmarx/kics-action@v1
      with:
        path: deployments/sky-deployment.yaml
        output_path: kics-results.json
        format: json

    - name: Install Minikube
      run: |
        curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
        chmod +x minikube-linux-amd64
        sudo mv minikube-linux-amd64 /usr/local/bin/minikube

    - name: Install kubectl
      run: |
        curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
        chmod +x kubectl
        sudo mv kubectl /usr/local/bin/

    - name: Start Minikube
      run: |
        minikube start --driver=docker

    - name: Set up Kubeconfig for Minikube
      run: |
        mkdir -p $HOME/.kube
        minikube update-context

    - name: Verify Kubernetes Context
      run: |
        kubectl config current-context

    - name: Deploy to Minikube
      run: |
        kubectl apply -f deployments/sky-deployment.yml